#!/bin/bash

# Pre-commit hook to validate tests
echo "Validating tests..."
node ./scripts/utils/validate-tests.js

# If validation fails, prevent commit
if [ $? -ne 0 ]; then
  echo "Test validation failed. Please fix the issues before committing."
  exit 1
fi

exit 0