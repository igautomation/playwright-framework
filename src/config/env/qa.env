# Base configuration
BASE_URL=https://qa.example.com
API_KEY=your-api-key-placeholder
HEADLESS=true
TIMEOUT_MULTIPLIER=1

# JIRA and Xray integration (set actual values if available)
JIRA_PROJECT_ID=PROJ-123
XRAY_TOKEN=your-xray-token-placeholder

# Test configuration
STORAGE_STATE=test-results/state.json
LOCALE=en-US
TIMEZONE=UTC

# Geolocation (already set, but included for completeness)
GEOLOCATION_LATITUDE=40.7128
GEOLOCATION_LONGITUDE=-74.0060

# Emulation settings (already set, but included for completeness)
OFFLINE=false
JAVASCRIPT_ENABLED=true

# Authentication and custom URLs
AUTH_TOKEN=mock-auth-token
CUSTOM_LOGIN_URL=/qa-login